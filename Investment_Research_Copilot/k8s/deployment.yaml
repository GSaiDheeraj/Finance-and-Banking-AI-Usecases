apiVersion: apps/v1
kind: Deployment
metadata:
  name: investment-research-api
  labels:
    app: investment-research-api
spec:
  # Single replica for now: uploaded data is written to the pod's local disk
  # (DATA_STORAGE_DIR, portfolio_monitor/api/routes.py::_save_uploads) — a second replica,
  # or even a rolling pod replacement, would not see data an earlier pod received.
  # Raise this once data storage moves to an object store or a ReadWriteMany PVC;
  # until then, scaling out would silently lose uploads that land on the "wrong" pod.
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: investment-research-api
  template:
    metadata:
      labels:
        app: investment-research-api
    spec:
      containers:
        - name: investment-research-api
          # Replaced by the CI pipeline with the actual built tag (ghcr.io/<owner>/<repo>:<sha>);
          # this placeholder is only here so the manifest is valid YAML on its own.
          image: ghcr.io/OWNER/REPO:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8000
          envFrom:
            - configMapRef:
                name: investment-research-config
            - secretRef:
                name: investment-research-secrets
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 5
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "1"
              memory: "1Gi"
