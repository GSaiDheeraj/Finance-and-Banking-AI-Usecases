# TEMPLATE ONLY — populate via kubectl or a secrets manager, never commit real values.
apiVersion: v1
kind: Secret
metadata:
  name: investment-research-secrets
  labels:
    app: investment-research-api
type: Opaque
stringData:
  PG_USER: "CHANGEME"
  PG_PASSWORD: "CHANGEME"
  LLM_ENDPOINT: "CHANGEME"
  LLM_API_KEY: "CHANGEME"
  LLM_MODEL_NAME: "CHANGEME"
