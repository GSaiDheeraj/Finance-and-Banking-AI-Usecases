# TEMPLATE ONLY — every value below is a placeholder. Never commit real credentials to
# this file. Populate the real Secret one of two ways instead:
#
#   kubectl create secret generic credit-risk-secrets \
#     --from-literal=PG_USER=... --from-literal=PG_PASSWORD=... \
#     --from-literal=LLM_API_KEY=... --from-literal=LLM_ENDPOINT=... \
#     --from-literal=LLM_MODEL_NAME=... --from-literal=EMBED_MODEL_NAME=...
#
# or manage it through your cluster's secrets manager (Sealed Secrets, External Secrets
# Operator + a real vault, etc.) and let that tooling generate this object — don't hand-
# edit real values into a YAML file that lives in version control.
apiVersion: v1
kind: Secret
metadata:
  name: credit-risk-secrets
  labels:
    app: credit-risk-api
type: Opaque
stringData:
  PG_USER: "CHANGEME"
  PG_PASSWORD: "CHANGEME"
  LLM_ENDPOINT: "CHANGEME"
  LLM_API_KEY: "CHANGEME"
  LLM_MODEL_NAME: "CHANGEME"
  EMBED_MODEL_NAME: "CHANGEME"
