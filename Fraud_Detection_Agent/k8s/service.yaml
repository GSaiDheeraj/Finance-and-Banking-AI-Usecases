apiVersion: v1
kind: Service
metadata:
  name: fraud-detection-api
  labels:
    app: fraud-detection-api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: fraud-detection-api
